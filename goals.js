import{$,id,money}from"./utils.js";import{store}from"./store.js";export function initGoals(){$("addGoalBtn")?.addEventListener("click",()=>{const name=$("goalName").value.trim(),target=Number($("goalTarget").value||0),saved=Number($("goalSaved").value||0);if(!name||target<=0)return alert("Preencha a meta.");store.addGoal({id:id(),name,target,saved});$("goalName").value="";$("goalTarget").value="";$("goalSaved").value=""});document.addEventListener("click",e=>{const b=e.target.closest("[data-del-goal]");if(b)store.removeGoal(b.dataset.delGoal)})}export function renderGoals(){$("goalList").innerHTML=store.state.goals.map(x=>{const pct=x.target?Math.min(100,Math.round(x.saved/x.target*100)):0;return`<div class="alert"><b>${x.name}</b><p>${money(x.saved)} de ${money(x.target)} • ${pct}%</p><div class="progress"><span style="width:${pct}%"></span></div><button data-del-goal="${x.id}">Excluir</button></div>`}).join("")||`<div class="alert">Nenhuma meta criada.</div>`}