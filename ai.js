/*
ai.js - camada futura da IA.
Mantém a IA local isolada do núcleo.
*/
window.RFAI = window.RFAI || {
  mode: "local-safe",
  describe() {
    return "IA local isolada. Próxima etapa: integração Cloud AI real via backend.";
  }
};
